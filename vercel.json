{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": null,
  "routes": [
    { "src": "/", "dest": "/sidepanel.html" },
    { "src": "/options", "dest": "/options.html" }
  ]
}
